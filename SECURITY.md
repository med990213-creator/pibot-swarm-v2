# 🛡️ Pi-Swarm Security Edition

نسخة أمنية متخصصة من OpenClaw مدمجة مع أدوات السرب.

## التثبيت السريع

```bash
git clone https://github.com/Pi-Swarm/pibot-swarm-v2.git
cd pibot-swarm-v2
pip install -r requirements.txt  # إذا وجدت حاجة لمكتبات خارجية
```

## المتطلبات

- Python 3.10+
- Ollama مثبت مع نموذج qwen2.5:1.5b
- nmap (للمسح الشبكي)

```bash
# تثبيت Ollama
curl -fsSL https://ollama.com/install.sh | sh
ollama pull qwen2.5:1.5b

# تثبيت nmap
sudo apt install nmap  # على Ubuntu/Debian
```

## طريقة التشغيل

### 1. فحص الحالة
```bash
python3 pi.py status
```

### 2. تدقيق ملف أو مشروع
```bash
# تدقيق ملف محلي
python3 pi.py audit ./my_code.py

# تدقيق مستودع
python3 pi.py audit https://github.com/user/repo
```

### 3. مسح شبكي
```bash
python3 pi.py scan 192.168.1.1
python3 pi.py scan 192.168.1.0/24
```

### 4. مهمة أمنية عامة
```bash
python3 pi.py task "ابحث عن ثغرات في المجلد الحالي"
python3 pi.py task "حلل هذا الكود واقترح تحسينات أمنية"
```

### 5. محادثة مباشرة مع الوكيل
```bash
python3 pi.py agent "ما هي أشهر ثغرات Solana؟"
```

## الأوامر المتاحة

| الأمر | الوصف | مثال |
|-------|-------|------|
| `status` | فحص حالة النظام والاتصال | `python3 pi.py status` |
| `audit <target>` | تدقيق أمني لملف أو مستودع | `python3 pi.py audit ./code.rs` |
| `scan <target>` | مسح شبكي وتحليل المنافذ | `python3 pi.py scan 192.168.1.1` |
| `task <description>` | مهمة أمنية عامة | `python3 pi.py task "ابحث عن SQLi"` |
| `agent <message>` | محادثة مع الوكيل | `python3 pi.py agent "نصيحة أمنية"` |

## الأدوات المدمجة

### 🔍 أدوات التحليل
- `audit_repo`: استنساخ وتحليل مستودعات GitHub
- `read_code`: قراءة وتحليل ملفات الكود
- `analyze_vulnerabilities`: البحث عن ثغرات شائعة

### 🌐 أدوات الشبكة
- `scan_target`: مسح المنافذ باستخدام nmap
- `service_detection`: اكتشاف الخدمات والإصدارات

### 🛠️ أدوات الإصلاح
- `write_patch`: كتابة ملفات التصحيح الأمني
- `generate_report`: إنشاء تقارير الأمان

### 🧠 الدمج مع الذكاء الاصطناعي
- جميع الأدوات تتصل بـ Ollama/Qwen للتحليل الذكي
- يدعم تحليل Solana، Rust، Python، JavaScript

## أمثلة عملية

### مثال 1: تدقيق عقد Solana
```bash
git clone https://github.com/example/solana-project.git
cd solana-project
python3 ~/pibot-swarm-v2/pi.py audit ./programs/my_program/src/lib.rs
```

### مثال 2: فحص الخادم المحلي
```bash
python3 ~/pibot-swarm-v2/pi.py scan localhost
```

### مثال 3: مهمة شاملة
```bash
python3 ~/pibot-swarm-v2/pi.py task "ابحث عن ثغرات UncheckedAccount في ملفات Rust الموجودة"
```

## البنية التقنية

```
pibot-swarm-v2/
├── pi.py                 # البوابة الرئيسية (Gateway)
├── pi_core/
│   ├── provider.py       # الاتصال بـ Ollama
│   ├── agent_runner.py   # منفذ المهام
│   └── tools.py          # مجموعة الأدوات الأمنية
└── SECURITY.md           # هذا الدليل
```

## التخصيص

يمكنك تعديل ملف `pi_core/tools.py` لإضافة أدوات أمنية جديدة.

## ملاحظات هامة

- جميع العمليات تتم **محلياً** عبر Ollama
- لا يتم إرسال بياناتك إلى خدمات خارجية
- يتطلب nmap فقط لأمر `scan`
